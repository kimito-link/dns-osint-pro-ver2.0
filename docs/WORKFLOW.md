# DNS OSINT Pro ver2.0 - 開発ワークフロー

最終更新: 2025-11-04

---

## 📋 目次

1. [開発環境のセットアップ](#開発環境のセットアップ)
2. [開発フロー](#開発フロー)
3. [Git ワークフロー](#git-ワークフロー)
4. [コーディング規約](#コーディング規約)
5. [テスト手順](#テスト手順)
6. [リリースフロー](#リリースフロー)
7. [トラブルシューティング](#トラブルシューティング)

---

## 🛠️ 開発環境のセットアップ

### 必要な環境

| ツール | バージョン | 用途 |
|--------|------------|------|
| Chrome | 88+ | テスト環境 |
| Git | 2.x | バージョン管理 |
| VSCode | 推奨 | エディタ |
| Node.js | 18+ | 開発ツール（オプション） |

### セットアップ手順

#### 1. リポジトリのクローン

```bash
# HTTPSでクローン
git clone https://github.com/yourusername/dns-osint-pro-ver2.0.git

# SSHでクローン（推奨）
git clone git@github.com:yourusername/dns-osint-pro-ver2.0.git

# ディレクトリ移動
cd dns-osint-pro-ver2.0
```

#### 2. Chrome拡張機能として読み込む

1. Chrome を開く
2. `chrome://extensions/` にアクセス
3. 右上の「デベロッパーモード」をON
4. 「パッケージ化されていない拡張機能を読み込む」をクリック
5. クローンしたフォルダ（`dns-osint-pro-ver2.0`）を選択

#### 3. VSCode拡張機能のインストール（推奨）

```json
{
  "recommendations": [
    "dbaeumer.vscode-eslint",
    "esbenp.prettier-vscode",
    "ritwickdey.liveserver",
    "usernamehw.errorlens"
  ]
}
```

#### 4. 開発用ツールのインストール（オプション）

```bash
# package.jsonがある場合
npm install

# または
yarn install
```

---

## 🔄 開発フロー

### 1. 新機能の開発

```
計画 → ブランチ作成 → コーディング → テスト → コミット → プッシュ → PR作成 → レビュー → マージ
```

#### ステップ1: 計画

1. **Issue作成**
   - GitHubで新しいIssueを作成
   - 機能の詳細を記述
   - ラベルを付ける（`feature`, `bug`, `enhancement`等）

2. **TODO.md更新**
   - タスクリストに追加
   - 優先度を設定

#### ステップ2: ブランチ作成

```bash
# 最新のmainを取得
git checkout main
git pull origin main

# 新しいブランチを作成
git checkout -b feature/your-feature-name

# 例
git checkout -b feature/subdomain-explorer
git checkout -b bugfix/suggest-api-timeout
```

#### ステップ3: コーディング

1. **コードを書く**
   - コーディング規約に従う
   - コメントを適切に追加
   - 関数・変数名は分かりやすく

2. **動作確認**
   - Chrome拡張機能をリロード（`chrome://extensions/` で更新）
   - 実際にテストサイトで動作確認

#### ステップ4: テスト

```bash
# 手動テスト
# 1. 拡張機能アイコンをクリック
# 2. 各機能を実行
# 3. エラーが出ないか確認
# 4. DevToolsでコンソールログを確認
```

#### ステップ5: コミット

```bash
# 変更をステージング
git add .

# コミット（分かりやすいメッセージ）
git commit -m "feat: サブドメイン探索機能を追加"

# または
git commit -m "fix: サジェストAPI のタイムアウトエラーを修正"
```

#### ステップ6: プッシュ

```bash
# リモートにプッシュ
git push origin feature/your-feature-name
```

#### ステップ7: Pull Request作成

1. GitHubでPR作成
2. タイトル・説明を記入
3. レビュワーを指定
4. ラベルを付ける

#### ステップ8: レビュー

- コードレビュー
- 動作確認
- 修正が必要な場合は追加コミット

#### ステップ9: マージ

```bash
# レビューが承認されたら main にマージ
# GitHub UI でマージ
# または
git checkout main
git merge feature/your-feature-name
git push origin main
```

---

## 🌿 Git ワークフロー

### ブランチ戦略

```
main
  ├── develop
  │     ├── feature/new-feature-1
  │     ├── feature/new-feature-2
  │     └── bugfix/fix-something
  └── hotfix/critical-bug
```

#### main ブランチ
- 本番環境（Chrome Web Store公開版）
- 常に安定した状態
- 直接コミット禁止

#### develop ブランチ（将来実装）
- 開発版
- 次のリリース候補
- feature/bugfix ブランチはここから分岐

#### feature/ ブランチ
- 新機能開発用
- `feature/機能名` の形式
- 例: `feature/subdomain-explorer`

#### bugfix/ ブランチ
- バグ修正用
- `bugfix/バグ内容` の形式
- 例: `bugfix/suggest-api-timeout`

#### hotfix/ ブランチ
- 緊急のバグ修正用
- main から直接分岐
- `hotfix/緊急度-バグ内容` の形式

### コミットメッセージ規約

#### プレフィックス

| プレフィックス | 意味 | 例 |
|---------------|------|-----|
| `feat:` | 新機能 | `feat: サブドメイン探索機能を追加` |
| `fix:` | バグ修正 | `fix: サジェストAPIのタイムアウトエラーを修正` |
| `docs:` | ドキュメント | `docs: README.mdを更新` |
| `style:` | コードフォーマット | `style: インデントを修正` |
| `refactor:` | リファクタリング | `refactor: DNS取得ロジックを整理` |
| `perf:` | パフォーマンス改善 | `perf: API呼び出しを最適化` |
| `test:` | テスト | `test: ユニットテストを追加` |
| `chore:` | その他 | `chore: package.jsonを更新` |

#### 例

```bash
# 良い例
git commit -m "feat: SSL証明書情報の詳細表示機能を追加"
git commit -m "fix: ネガティブキーワード検出の誤判定を修正"
git commit -m "docs: API.mdにRDAP情報を追加"

# 悪い例
git commit -m "update"
git commit -m "fix bug"
git commit -m "修正"
```

---

## 📝 コーディング規約

### JavaScript

#### インデント
```javascript
// スペース2個
function exampleFunction() {
  if (condition) {
    doSomething();
  }
}
```

#### 命名規則

```javascript
// camelCase（変数・関数）
const userName = "りんく";
function getUserData() { }

// PascalCase（クラス）
class DnsResolver { }

// UPPER_SNAKE_CASE（定数）
const API_ENDPOINT = "https://cloudflare-dns.com/dns-query";
const MAX_RETRY_COUNT = 3;
```

#### セミコロン

```javascript
// セミコロンを使う
const domain = "example.com";
console.log(domain);
```

#### クォート

```javascript
// シングルクォート推奨
const message = 'Hello World';

// ダブルクォートはHTMLやJSON内で使用
const html = "<div class='example'></div>";
```

#### アロー関数

```javascript
// アロー関数を使う（ES6+）
const add = (a, b) => a + b;

// 複数行の場合
const processData = (data) => {
  const result = transform(data);
  return result;
};
```

#### async/await

```javascript
// async/awaitを使う
async function fetchDnsData(domain) {
  try {
    const response = await fetch(url);
    const data = await response.json();
    return data;
  } catch (error) {
    console.error('エラー:', error);
    throw error;
  }
}
```

#### コメント

```javascript
// 単一行コメント: 日本語OK

/**
 * 複数行コメント: 関数の説明
 * @param {string} domain - ドメイン名
 * @returns {Promise<Object>} DNS情報
 */
async function getDnsInfo(domain) {
  // 実装
}
```

### HTML

#### インデント

```html
<!-- スペース2個 -->
<div class="container">
  <h1>タイトル</h1>
  <p>本文</p>
</div>
```

#### 属性

```html
<!-- ダブルクォート使用 -->
<input type="text" id="domainInput" class="form-control">

<!-- 属性は改行しない（短い場合） -->
<button class="btn" onclick="handleClick()">クリック</button>

<!-- 属性が多い場合は改行 -->
<button
  class="btn btn-primary"
  data-action="submit"
  onclick="handleSubmit()"
>
  送信
</button>
```

### CSS

#### インデント

```css
/* スペース2個 */
.container {
  display: flex;
  flex-direction: column;
}
```

#### セレクタ

```css
/* kebab-case（ハイフン区切り） */
.search-button {
  background-color: #4a90e2;
}

/* IDセレクタは避ける（再利用性） */
#domain-input { /* あまり使わない */ }
```

#### プロパティ順序

```css
/* 推奨順序 */
.element {
  /* 配置 */
  display: flex;
  position: relative;
  
  /* ボックスモデル */
  width: 100%;
  height: 50px;
  margin: 10px;
  padding: 5px;
  
  /* 装飾 */
  background-color: #fff;
  border: 1px solid #ccc;
  color: #333;
  
  /* テキスト */
  font-size: 14px;
  line-height: 1.6;
  text-align: center;
  
  /* その他 */
  cursor: pointer;
  transition: all 0.3s;
}
```

---

## 🧪 テスト手順

### 手動テスト

#### 基本動作テスト

1. **拡張機能の起動**
   - [ ] アイコンクリックでポップアップ表示
   - [ ] UI が正常に表示される

2. **自動ドメイン取得**
   - [ ] 現在のタブのドメインが自動入力される
   - [ ] 正しいドメイン名が抽出される

3. **DNS情報取得**
   - [ ] Aレコード取得成功
   - [ ] MXレコード取得成功
   - [ ] NSレコード取得成功
   - [ ] TXTレコード取得成功

4. **WHOIS/RDAP情報取得**
   - [ ] ドメイン情報取得成功
   - [ ] IP情報取得成功
   - [ ] エラーハンドリング動作確認

5. **サジェスト分析**
   - [ ] Google サジェスト取得
   - [ ] Yahoo! サジェスト取得
   - [ ] Bing サジェスト取得
   - [ ] ネガティブキーワード検出
   - [ ] 風評被害アラート表示

6. **サイト健康診断**
   - [ ] WordPress検出
   - [ ] HTTPS確認
   - [ ] セキュリティヘッダー解析
   - [ ] キャラクター吹き出し表示

7. **メールセキュリティ診断**
   - [ ] SPFレコード確認
   - [ ] DKIMレコード確認
   - [ ] DMARCレコード確認

8. **エクスポート機能**
   - [ ] テキストコピー
   - [ ] JSONダウンロード

#### テストケース

| テストケース | 期待結果 | 実際の結果 | 合否 |
|-------------|----------|-----------|------|
| example.com のDNS取得 | Aレコード取得成功 |  | ✅/❌ |
| 存在しないドメインの検索 | エラーメッセージ表示 |  | ✅/❌ |
| ネガティブキーワード検出 | 赤字ハイライト表示 |  | ✅/❌ |
| WordPress検出 | バージョン表示 |  | ✅/❌ |

### デバッグ方法

#### 1. コンソールログの確認

```javascript
// ログ出力
console.log('DNS情報:', dnsData);
console.error('エラー:', error);
console.warn('警告:', warning);
```

**確認方法:**
- ポップアップを右クリック → 「検証」
- DevTools のコンソールタブを確認

#### 2. バックグラウンドスクリプトのデバッグ

**確認方法:**
- `chrome://extensions/` を開く
- 「service worker」をクリック
- DevTools が開く

#### 3. ネットワーク通信の確認

**確認方法:**
- DevTools → Networkタブ
- API呼び出しを確認
- レスポンスを確認

#### 4. ストレージの確認

**確認方法:**
- DevTools → Applicationタブ
- Storage → Local Storage
- chrome.storage の内容を確認

---

## 🚀 リリースフロー

### バージョン番号の付け方

**セマンティックバージョニング: MAJOR.MINOR.PATCH**

```
5.5.0
│ │ │
│ │ └─ PATCH: バグ修正
│ └─── MINOR: 新機能追加（後方互換性あり）
└───── MAJOR: 大きな変更（後方互換性なし）
```

#### 例

| バージョン | 変更内容 | 理由 |
|-----------|---------|------|
| 5.0.0 → 5.1.0 | サイト健康診断機能追加 | 新機能（MINOR） |
| 5.1.0 → 5.1.1 | サジェストAPIのバグ修正 | バグ修正（PATCH） |
| 5.5.0 → 6.0.0 | API仕様の大幅変更 | 破壊的変更（MAJOR） |

### リリース手順

#### 1. バージョンアップ準備

```bash
# 最新のmainを取得
git checkout main
git pull origin main

# リリースブランチ作成
git checkout -b release/v5.6.0
```

#### 2. バージョン番号の更新

**manifest.json**
```json
{
  "version": "5.6.0"
}
```

**package.json**（ある場合）
```json
{
  "version": "5.6.0"
}
```

#### 3. CHANGELOG.md の更新

```markdown
## [5.6.0] - 2025-11-10

### 追加
- サブドメイン探索機能
- SSL証明書情報の詳細表示

### 修正
- サジェストAPIのタイムアウトエラー

### 変更
- UIの配色を調整
```

#### 4. ドキュメントの更新

- README.md のバージョン番号
- docs/ 内のドキュメント

#### 5. コミット・タグ付け

```bash
# 変更をコミット
git add .
git commit -m "chore: v5.6.0 リリース準備"

# タグ付け
git tag -a v5.6.0 -m "v5.6.0 リリース"

# プッシュ
git push origin release/v5.6.0
git push origin v5.6.0
```

#### 6. Pull Request & マージ

1. GitHubでPR作成
2. レビュー
3. main にマージ

#### 7. Chrome Web Store公開

1. `chrome://extensions/` を開く
2. 「パッケージ化」をクリック
3. ZIPファイルを作成
4. Chrome Web Store Developer Dashboardにアップロード
5. 審査申請

#### 8. GitHub Release

1. GitHubのReleasesページへ
2. 新しいリリースを作成
3. タグを選択（v5.6.0）
4. リリースノートを記入
5. ZIPファイルを添付
6. 公開

---

## 🔧 トラブルシューティング

### よくある問題と解決方法

#### 1. 拡張機能が読み込めない

**症状:**
- 「マニフェストが見つかりません」エラー

**解決方法:**
```bash
# manifest.jsonがあるか確認
ls -la manifest.json

# manifest.json の文法エラーチェック
# JSONフォーマットツールで確認
```

#### 2. API呼び出しが失敗する

**症状:**
- DNS情報が取得できない
- "Failed to fetch" エラー

**解決方法:**
```javascript
// CORSエラーの可能性
// background.js でAPI呼び出しを行う

// タイムアウト設定を追加
const controller = new AbortController();
const timeoutId = setTimeout(() => controller.abort(), 5000);

fetch(url, { signal: controller.signal })
  .then(response => response.json())
  .finally(() => clearTimeout(timeoutId));
```

#### 3. ポップアップが表示されない

**症状:**
- アイコンクリックで何も起こらない

**解決方法:**
```bash
# DevToolsで確認
# 右クリック → 検証

# エラーログを確認
# コンソールタブを確認
```

#### 4. ストレージが保存されない

**症状:**
- 履歴が保存されない

**解決方法:**
```javascript
// chrome.storage.local の使用方法を確認
chrome.storage.local.set({ key: value }, () => {
  if (chrome.runtime.lastError) {
    console.error('保存エラー:', chrome.runtime.lastError);
  }
});
```

#### 5. background.js が動作しない

**症状:**
- Service Workerが起動しない

**解決方法:**
```bash
# chrome://extensions/ を開く
# Service Workerの「エラー」を確認
# 「更新」ボタンをクリック
```

---

## 📚 参考リンク

### 公式ドキュメント

- [Chrome Extensions Documentation](https://developer.chrome.com/docs/extensions/)
- [Manifest V3 Migration Guide](https://developer.chrome.com/docs/extensions/mv3/intro/)
- [Chrome Storage API](https://developer.chrome.com/docs/extensions/reference/storage/)
- [Chrome Runtime API](https://developer.chrome.com/docs/extensions/reference/runtime/)

### 開発ツール

- [Chrome Extension CLI](https://github.com/dutiyesh/chrome-extension-cli)
- [Extension Reloader](https://chrome.google.com/webstore/detail/extensions-reloader/)

### コミュニティ

- [Stack Overflow - Chrome Extension](https://stackoverflow.com/questions/tagged/google-chrome-extension)
- [Reddit - r/chrome_extensions](https://www.reddit.com/r/chrome_extensions/)

---

## 📧 サポート

開発中に問題が発生した場合：

- **Email**: info@reverse-re-birth-hack.com
- **GitHub Issues**: [リンク]
- **公式サイト**: https://reverse-rebirth-hack.com

---

<div align="center">

**DNS OSINT Pro ver2.0 - Development Workflow**

君と繋がる、WEBサイト健康診断＆OSINT調査ツール

最終更新: 2025-11-04

</div>
