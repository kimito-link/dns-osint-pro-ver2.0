# 🔍 Ubersuggest拡張機能 - 詳細分析レポート

**分析日**: 2025年11月8日  
**バージョン**: 4.0.25.6383_0  
**拡張機能ID**: nmpgaoofmjlimabncmnmnopjabbflegf

---

## 📊 **分析サマリー**

Ubersuggest拡張機能を徹底的に分析しました。以下は重要な発見です。

### 🎯 **主要機能の発見**

#### 1. **キーワードデータ構造**

contentScript.bundle.js の1171行目で発見：

```javascript
vW=[
  {type:"text", title:"seo_keywords", style:{width:"40%", padding:"16px 6px"}},
  {type:"text", title:"volume", style:{width:"16%", padding:"16px 6px"}},
  {type:"text", title:"position", style:{width:"20%", textAlign:"center", padding:"16px 6px"}},
  {type:"text", title:"est_visits", style:{width:"20%", padding:"16px 6px"}}
]
```

**データ構造**:
- **seo_keywords**: SEOキーワード（40%幅）
- **volume**: 検索ボリューム（16%幅）
- **position**: ランキング順位（20%幅、中央揃え）
- **est_visits**: 推定訪問数（20%幅）

---

#### 2. **APIエンドポイント**

manifest.jsonから判明：

```json
"host_permissions": [
  "https://green.api.neilpatel.com/*",
  "https://blue.api.neilpatel.com/*",
  "https://app.neilpatel.com/*"
]
```

**エンドポイント構造**（コードから推測）:
- `/traffic_analyzer/keywords` - キーワード分析
- `/traffic_analyzer/overview` - トラフィック概要
- `/ubersuggest/overview` - Ubersuggest概要
- `/seo_analyzer/backlinks` - バックリンク分析

---

#### 3. **ファイル構成**

```
├── manifest.json (1,779 bytes)
├── background.bundle.js (579 KB) - バックグラウンド処理
├── contentScript.bundle.js (2.7 MB) - メインロジック
├── popup.bundle.js (1 MB) - ポップアップUI
├── options.bundle.js (1.1 MB) - オプション設定
├── content.styles.css (10 KB) - スタイル
├── lib/jquery.min.js
└── images/ - アイコン・画像
```

---

#### 4. **UI構造**

**サイドバー**:
```css
.ue-sidebar-container {
  width: 607px !important;
  height: 100vh;
  position: fixed;
  top: 0px;
  right: -607px;
  z-index: 100000000000000;
  transition: all 0.5s ease-in-out;
}
```

**右側セクション**:
```css
.ue-right-section {
  width: 482px; /* @media min-width: 1364px */
  border: 1px solid #dee1e5;
  border-radius: 10px;
  background-color: #fff;
}
```

---

#### 5. **主要機能の実装パターン**

contentScript.bundle.jsの分析から：

**A. キーワードデータ取得**:
- `loadSeoKeywords(locationId, lang)` - SEOキーワードの非同期取得
- パラメータ: `domain`, `locId`, `lang`, `mode`

**B. データ表示**:
```javascript
// 検索ボリュームのフォーマット
Rt(volume, false, 2, {lang:"en", locId:2840}, true, 10000)

// テーブル構造
- seo_keywords: キーワード表示 + クリック可能リンク
- volume: フォーマットされた検索ボリューム
- position: ランキング順位
- est_visits: 推定訪問数
```

**C. 国別フィルタリング**:
```javascript
Wt(e.data.locationId) // 国コードから国情報を取得
// 国旗アイコン表示
<Yl countryCode={countryIsoCode} style={{width:"24px"}} svg={true} />
```

---

#### 6. **ダークテーマ対応**

```css
/* YouTube ダークテーマ */
html[dark] .ue-right-section {
  background: #212121;
  border-color: #212121;
  color: #fff;
}

/* Google ダークテーマ */
body[data-dt='1'] .ue-right-section {
  background: #2a2a2a;
  border-color: #2a2a2a;
  color: #fff;
}
```

---

#### 7. **対応プラットフォーム**

- **Google検索** - 検索結果ページにサイドバー表示
- **YouTube** - 動画ページにキーワード情報表示
- **Amazon** - 商品ページにキーワード情報表示

---

## 💡 **あなたのプログラムへの適用提案**

### 実装可能な機能

#### 1. **キーワードテーブル表示**

Ubersuggestと同様のテーブル構造を作成：

```javascript
const keywordColumns = [
  { title: "キーワード", width: "40%", key: "keyword" },
  { title: "ボリューム", width: "20%", key: "volume" },
  { title: "難易度", width: "20%", key: "difficulty" },
  { title: "関連度", width: "20%", key: "relevance" }
];
```

#### 2. **サジェストデータの可視化**

既存のサジェストデータを以下のように表示：

```javascript
// 既存のサジェストデータ
google: ["yahoo mail", "yahoo finance", "yahoo news", ...]

// 新しい表示形式
[
  { keyword: "yahoo mail", volume: "推定", relevance: "高" },
  { keyword: "yahoo finance", volume: "推定", relevance: "高" },
  { keyword: "yahoo news", volume: "推定", relevance: "中" }
]
```

#### 3. **スタイル改善**

Ubersuggest風のモダンなUI：

```css
.keyword-table {
  border: 1px solid #dee1e5;
  border-radius: 10px;
  background-color: #fff;
  font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
}

.keyword-row {
  padding: 16px 6px;
  border-bottom: 1px solid #f0f0f0;
  transition: background-color 0.2s;
}

.keyword-row:hover {
  background-color: #f8f9fa;
}
```

#### 4. **ネガティブキーワードの視覚的強調**

```javascript
// ネガティブキーワードを赤で表示
if (isNegative) {
  element.style.color = '#d32f2f';
  element.style.fontWeight = '600';
}
```

---

## 🚀 **次のステップ**

### オプションA: Ubersuggest風のUI実装
1. キーワードテーブルコンポーネント作成
2. サジェストデータの構造化
3. モダンなスタイル適用
4. ソート・フィルタリング機能追加

### オプションB: より深い分析
1. ネットワークタブでAPI呼び出しを傍受
2. 実際のレスポンスデータ構造を確認
3. APIの動作を完全に理解

### オプションC: 独自機能の開発
Ubersuggestにはない機能：
- **リアルタイムサジェスト更新**
- **複数検索エンジン同時比較**
- **ネガティブキーワード自動検出**
- **ドメイン情報との統合表示**

---

## 📝 **技術的な制約**

### Ubersuggestが使用している技術

1. **React.js** - UIフレームワーク
   ```
   /** @license React v16.13.1 */
   ```

2. **styled-components** - CSS-in-JS
   ```javascript
   const StyledComponent = styled.div`...`
   ```

3. **axios** - HTTP クライアント（バンドルされている）

4. **lodash** - ユーティリティライブラリ

5. **moment.js** - 日付処理

6. **Chart.js** - グラフ表示

---

## ⚠️ **重要な発見**

### プライバシーとデータ

Ubersuggestは以下のデータにアクセス：

1. **閲覧履歴**: 検索ページのキーワードを読み取り
2. **Cookie**: ユーザー認証用
3. **外部API**: Neil Patel のサーバーにデータ送信

### レート制限

- フリープラン: 日次制限あり
- API呼び出しは認証が必要

---

## 🎨 **UIデザインパターン**

### カラースキーム

```css
--orange: #FF6B35;
--orange10: rgba(255, 107, 53, 0.1);
--orange30: rgba(255, 107, 53, 0.3);
--green: #4CAF50;
--red: #d32f2f;
--darkGray: #212121;
--lightGray: #f0f0f0;
--white: #ffffff;
```

### タイポグラフィ

```css
font-family: 'Figtree', sans-serif;
font-weight: 400 (Regular), 500 (Medium), 600 (SemiBold), 700 (Bold);
```

---

## 📈 **実装の優先度**

### 🔴 高優先度（すぐ実装可能）
1. キーワードテーブル表示
2. サジェストデータの構造化
3. ネガティブキーワードの視覚的強調

### 🟡 中優先度（設計が必要）
4. ソート・フィルタリング機能
5. 検索ボリューム推定機能
6. 国別フィルタリング

### 🟢 低優先度（将来的に）
7. グラフ表示
8. トレンド分析
9. API統合

---

## ✅ **結論**

Ubersuggestの核心機能は：

1. **構造化されたキーワードデータ表示**
2. **API経由での検索ボリューム取得**
3. **視覚的に優れたテーブルUI**
4. **プラットフォーム別の最適化**

**あなたのプログラムは既に以下を持っています**：
- ✅ サジェストデータ取得（Google, Yahoo, Bing）
- ✅ ネガティブキーワード検出
- ✅ ドメイン情報取得

**次に追加すべきもの**：
- 📊 キーワードテーブル表示
- 🎨 モダンなUI
- 📈 データの構造化

---

**推奨**: Ubersuggestの完全なコピーではなく、**既存機能を拡張**して、Ubersuggest風の視覚的に優れた表示を追加することをお勧めします！
